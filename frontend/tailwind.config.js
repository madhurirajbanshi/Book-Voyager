/** @type {import('tailwindcss').Config} */
export default {
   content: [
    './index.html', // if you have an HTML file at the root
    './src/**/*.{js,jsx,ts,tsx}', // for React components, JS, and TS files
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

